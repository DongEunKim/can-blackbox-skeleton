# 단계 7: 저장소 관리 검증

## 동작

- `[storage]` max_total_mb 초과 시 오래된 파일(mtime 기준)부터 삭제
- 업로더의 `after_scan`에서 매 스캔 후 `trim_storage` 호출
- 0바이트 파일은 삭제 대상에서 제외

## 단위 테스트

```bash
python3 -m pytest tests/test_storage_manager.py -v
```
